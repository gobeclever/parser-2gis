# This model is no longer used — the "nearest station" (metro) columns were
# removed from the output on purpose. The raw `nearest_stations` field still
# comes through untouched in JSON/JSONL output if you need it later.
# Safe to delete this file.
